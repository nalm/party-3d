# party-3d — 정당 3축 탐색기

경제 × 사회·문화 × 민주주의 규범 3축 공간에서 각국 정당의 위치를 회전·확대하며 탐색하는 정적 웹앱.
GitHub Pages 배포. 오마이뉴스 편집국 및 대외 공개용.

**작업 전 반드시 `docs/framework-spec.md`를 읽을 것.** 이 파일은 그 명세의 구현 지침이며, 두 문서가 충돌하면 명세가 우선한다.

---

## 절대 규칙

1. **세 축은 독립이다.** 반지름·중심으로부터의 거리로 "극단성"을 표현하지 말 것. 경제 8·문화 5·규범 0.2인 정당과 경제 5·문화 5·규범 0.9인 정당이 같은 값으로 보여서는 안 된다.
2. **공간은 와이어프레임 정육면체.** 구체(sphere) 금지. 구 형태는 "중심에서의 거리 = 극단성"을 암시하므로 1번과 충돌한다.
3. **척도를 정규화하지 말 것.** X·Y는 0–10, Z는 0–1이다. 하나의 스케일로 합치면 Z의 임계선 성격이 사라진다. 화면 좌표로 매핑할 때만 각각 독립적으로 스케일한다.
4. **모든 점은 선택 시 출처를 표시해야 한다.** 데이터셋 · 판 · 변수명 · 연도 · 척도 범위. 선택 사항이 아니다.
5. **`estimated: true`인 값은 시각적으로 구분한다.** 점선 테두리 또는 반투명. 현재 시드 데이터는 좌표 전부가 추정치이므로 앱 어딘가에 상시 경고 배너가 필요하다.
6. **색만으로 범주를 구분하지 말 것.** 규범 구간은 색 + 도형을 함께 쓴다.
7. **연도 없는 좌표는 렌더링하지 않는다.** 정당 위치는 고정이 아니다.

---

## 스택

| 항목 | 선택 | 비고 |
|---|---|---|
| 3D | Three.js 최신 안정판 | `OrbitControls`, `CSS2DRenderer` 사용 |
| 빌드 | Vite | |
| 언어 | 바닐라 JS (ES modules) | TypeScript 불필요. 규모가 작다 |
| 배포 | GitHub Pages | `vite.config.js`에 `base: '/party-3d/'` |
| 의존성 | 최소화 | 상태관리 라이브러리·UI 프레임워크 도입 금지 |

### 기술 주의사항

- **라벨은 `CSS2DRenderer`로 HTML 오버레이 처리.** 텍스처·SDF 폰트 방식은 한글 글리프 때문에 번거롭다. HTML 라벨이면 브라우저 폰트가 한글을 그대로 처리하고 선명하다.
- **점 개수는 최대 수십 개다.** `InstancedMesh`나 GPU 최적화 불필요. 명료성과 가독성을 우선한다.
- 이전에 아티팩트 환경에서 Three.js r128에 `OrbitControls`가 없던 제약이 있었으나, **이 프로젝트에는 해당하지 않는다.** 최신 버전을 그대로 쓴다.

---

## 디렉터리 구조

```
party-3d/
  CLAUDE.md
  docs/
    framework-spec.md      명세 (권위 문서)
    coding-rubric.md        축별 코딩 루브릭 (작성 완료)
    kickoff-prompt.md       단계별 작업 지시
    정당분류_3축_프레임워크_3D.svg   시각 기준 도해
  data/
    parties.json            정당 좌표 (현재 시드)
    sources.json            데이터셋 메타 — 패널 출처 표시가 참조
  src/
    main.js                 엔트리, 씬 구성
    scene.js                카메라 · 렌더러 · 컨트롤 · 프레이밍
    axes.js                 큐브 · 축 · 눈금 · 격자 · 좌표 매핑
    points.js               정당 점 · 드롭라인 · 인코딩
    pick.js                 호버 · 선택 레이캐스팅
    labels.js               CSS2D 라벨
    panel.js                선택 상세 패널
    panel-source.js         출처 블록 렌더링 (패널에서 분리)
    dom.js                  el · kv DOM 조립 헬퍼
    filters.js              국가 · 가족 · 연도 필터
    trajectory.js           연도별 이동 화살표
    view2d.js               2D 산점도 (Canvas 2D)
    export-png.js           PNG 합성 (경고 · 범례 · 출처 각인)
    ui.js                   뷰 전환 · 프리셋 · 절단점 슬라이더 · 궤적 토글 · 리셋
    config.js               색 · 도형 · 구간 절단점 · 한국어 문자열
  index.html
  vite.config.js
```

---

## 데이터 계약

`data/parties.json` 구조는 `docs/framework-spec.md` 9절이 정본이다. 코드에서 가정할 것:

- 최상위는 `{ "_meta": {...}, "parties": [...] }`
- 각 레코드의 `econ` · `cultural` · `norms`는 `{ v, scale, src, estimated }`를 반드시 가진다
- `norms.band`는 `pluralist` | `borderline` | `anti_pluralist`
- **같은 정당의 여러 연도는 `party_key`로 묶인다.** `id`는 레코드 고유, `party_key`는 정당 고유
- `cultural.sub`는 한국 정당에서만 채워진다(대북·안보 / 젠더·소수자 / 전통가치·종교 하위 점수). null 허용

### 구간 절단점은 하드코딩 금지

`_meta.norms_bands`의 0.30 / 0.50은 명세의 제안값이며 문헌 근거가 없다. **`src/config.js`에 설정값으로 노출하고 UI에서 조정 가능하게 한다.**

### 데이터 로딩 시 검증

`parties.json`을 읽은 뒤 다음을 확인하고, 위반 시 콘솔 경고를 남긴다.

- 필수 필드 존재
- `v`가 `scale` 범위 안에 있는지
- `norms.band`가 `v`와 일치하는지
- `party_key` 중복 연도가 없는지

---

## 시각화 규칙

### 3D 뷰

- **드롭 라인 필수.** 각 점에서 X-Z 평면까지 수직선. 깊이를 읽는 가장 강력한 단서이며, 이것이 없으면 3D는 판독 불가다.
- **라벨은 호버·선택 시에만.** 상시 표시하면 겹쳐서 읽을 수 없다. 예외적으로 선택된 점과 궤적 양 끝점은 유지한다.
- **프리셋 카메라 버튼 3개**: `경제 × 문화 정면`(= 2D 산점도와 동일), `규범 정면`, `등축`.
- 배경 격자는 깊이 참조용이며 **아무 의미도 없다**는 것을 UI에 명시한다.
- 축 라벨은 양극 모두 표시한다. 각 축은 스펙트럼이므로 단방향 화살표는 부적절하다.

### 인코딩

| 규범 구간 | 색 | 도형 |
|---|---|---|
| 다원주의 (0.00–0.30) | `#4a9eff` | 구 |
| 경계 (0.30–0.50) | `#eda100` | 8면체 |
| 반다원주의 (0.50–1.00) | `#d03b3b` | 4면체 |

축 색: 경제 `#2a78d6` · 사회문화 `#1baf7a` · 민주주의 규범 `#eb6834`

**해소됨 (2026-07-30).** 원래 다원주의 구간과 경제축이 같은 `#2a78d6`이었다. 1~2단계 구현 후 프레임버퍼 픽셀 검사에서 두 색이 분리되지 않는 것을 확인해 다원주의를 `#4a9eff`로 밝혔다. 재질 구분(축은 불투명 라인, 점은 광택 있는 입체 + 조명)도 함께 유지한다. **경제축 색은 `#2a78d6`으로 그대로 두었다** — 명세와 도해(`docs/정당분류_3축_프레임워크_3D.svg`)의 축 색이 이 값이다.

### 궤적

같은 `party_key`의 연도별 이동을 화살표로 표시한다. **이 앱의 최대 강점이 여기 있다.**
검증 사례 (CHES 실측, 2026-07-30 교체 후): Fidesz 2010→2019는 규범축이 0.40 → 0.88(추정치)로 크게 움직이는데 경제축은 3.41 → 3.73으로 거의 고정이다. 이 패턴이 화면에서 드러나야 한다.
**주의:** 시드 추정치는 Fidesz·PiS를 경제 중도우파(6.3, 5.5)로 놓았으나 CHES 실측은 좌측(3.4, 3.1)이다. 교체 전 문서·대화에 남아 있는 옛 수치를 신뢰하지 말 것.

### 2D 대체 뷰

X-Y 산점도 + 규범을 색·도형으로 인코딩한 2D 뷰를 토글로 제공한다. **보도·인쇄용 기본값은 2D다.** 정적 이미지에 점을 찍으면 독자가 깊이를 판독할 수 없다.

---

## 개발

```bash
npm install
npm run dev        # 로컬 개발
npm run build      # dist/ 생성
npm run preview    # 빌드 결과 확인
```

배포는 `dist/`를 `gh-pages` 브랜치로. `vite.config.js`의 `base`가 저장소 이름과 일치하는지 확인할 것 — 불일치하면 GitHub Pages에서 에셋 404가 난다.

---

## 코드 컨벤션

- 한국어 UI 문자열은 `src/config.js`에 모아둔다. 코드에 하드코딩하지 말 것
- 매직 넘버 금지. 축 길이·점 크기·카메라 거리 등은 `config.js`
- 주석은 한국어. "무엇을"이 아니라 "왜"를 쓴다
- 파일당 200줄을 넘으면 분할 검토

---

## 하지 말 것

- 구체(sphere) 형태의 공간
- 세 축을 하나의 스케일로 정규화
- 출처 표시 없는 점
- 상시 표시되는 전체 라벨
- 색만으로 하는 범주 구분
- 상태관리 라이브러리 · UI 프레임워크 도입
- 추정치와 실측값을 시각적으로 동일하게 표현
- 구간 절단점 하드코딩

---

## 작업 전 물어볼 것

다음은 임의로 결정하지 말고 확인한다.

- 명세와 이 파일이 충돌하는 것처럼 보일 때
- 데이터 스키마를 변경해야 할 때 (`framework-spec.md` 9절도 함께 고쳐야 한다)
- 새 의존성을 추가할 때
- 축의 의미·척도·구간 절단점을 바꿔야 할 때

---

## 단계 계획

`docs/kickoff-prompt.md`에 단계별 지시문이 있다. 한 번에 전부 구현하지 말 것.
Three.js는 카메라·스케일 문제가 초반에 자주 나므로, **점이 화면에 보이는 상태를 먼저 확정**해야 이후 디버깅이 쉽다.

| 단계 | 내용 | 완료 조건 |
|---|---|---|
| 1 | 큐브 · 축 · 눈금 · OrbitControls | 회전·확대가 되고 축 양극 라벨이 보인다 |
| 2 | 데이터 로딩 · 점 · 드롭라인 · 인코딩 | 20개 점이 구간별 색·도형으로 보인다 |
| 3 | 라벨 · 호버 · 선택 · 상세 패널 | 클릭하면 출처가 표시된다 |
| 4 | 필터 · 카메라 프리셋 | 국가·가족·연도로 걸러진다 |
| 5 | 궤적 화살표 | Fidesz의 수직 이동이 보인다 |
| 6 | 2D 토글 · 배포 | GitHub Pages에서 열린다 |

---

## 현재 상태 경고

**2026-07-30 CHES 교체 완료.** 유럽 18개 레코드의 econ·cultural(36개 좌표)은 CHES 1999–2024 트렌드 파일 V2 실측값이다. 추출은 `scripts/apply-ches.mjs`, 매칭 근거는 `data/crosswalk.json`(Party Facts ID).
**여전히 추정치인 것:** 규범축 전체(20개) — V-Party 원본이 아닌 2차 문헌 인용값. 한국 정당 좌표 전체(6개). **규범축 수치와 한국 좌표는 인용 금지.**
V-Party 교체는 V-Dem 다운로드 양식(이메일 + 개인정보 동의) 제출 승인 대기 중이다. 연도 규칙은 결정됨: 최근접 선거 채택 + 관측연도 src 명시 + 2년 초과 시 note 경고.

### 공개 조건 (2026-07-30 편집국 결정)

원래 규칙은 "실측 교체 전 대외 공개 금지"였다. 명세 2절도 B안(한국 포함)을 "대외 공개에 부적합"으로 규정한다.
**2026-07-30 편집국 결정으로 이 금지를 조건부 공개로 완화했다.** 결정 이력을 지우지 말 것. 조건은 다음 세 가지이며 하나라도 빠지면 공개하지 않는다.

1. 화면에 **상시 고정** 경고. 원래 상단 대형이었으나 CHES 실측 교체 후 2026-07-30 편집국 결정으로 **하단 소형 바**로 완화했다 (문구: "규범축·한국 좌표는 추정치 — 인용 주의", 전문은 툴팁). 규범축·한국 좌표까지 실측·확정되기 전에는 제거하지 않는다
2. PNG 내보내기 시 **이미지 자체에** 경고 각인 — 이쪽은 완화하지 않는다. 이미지는 캡션 없이 단독 유통되기 때문
3. `index.html`에 `<meta name="robots" content="noindex">`

`gh-pages` 푸시는 자동화하지 않는다. 6단계에서 빌드·검증까지만 하고 **푸시 직전에 사람에게 확인받는다.**

### 한국 정당 레코드 주의

`kr-ppp-2025` · `kr-dp-2025`는 명세 2절 B안으로 추가한 추정 레코드다.

- `cultural.v`는 명세 4.3절 하위 3지표의 균등 가중합 — 계산값이라 이견 여지가 작다
- `econ.v`는 참조 위치 사이 보간 — **이 파일에서 근거가 가장 약한 값이다**
- `norms.v`는 루브릭 Z-1~Z-4 코딩 **미착수** 상태의 잠정 판정. 증거 로그(루브릭 8절)가 없다
- `kr-ppp-2025`의 `norms.v` = 0.55는 반다원주의 구간에 들어가지만, 명세 12.1절의 권장 서술은 단정을 피하라고 한다. **UI에서 구간 라벨만 읽고 "반다원주의 정당"이라 서술하면 명세 위반이다.** 레코드 `note`에 이 긴장을 적어 두었고, 상세 패널이 반드시 함께 노출해야 한다
